function init () {
    var firstName = "ram";
    function sayHello () {
        console.log(firstName);
    }
    sayHello();
}

init();

//  lexical scope basically represent the scope of the variable inside the function, outside that there is no existence of it .
